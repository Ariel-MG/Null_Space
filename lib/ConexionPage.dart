import 'package:flutter/material.dart';

class ConexionPage extends StatelessWidget {
  const ConexionPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Image(image: AssetImage('assets/conexionimage.png'));
  }
}